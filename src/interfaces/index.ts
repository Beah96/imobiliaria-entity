import { IUser, IUserCreate, IUserList, IUserReturn, IUserUpdate } from "./user.interface";
import { ILogin, ILoginReturn } from "./login.interface";
import { ICategory, ICategoryList, ICreateCategory } from "./category.interface";
import { IRealEstate, IRealEstateList, IRealEstateCreate } from "./realEstate.interface";
import { IAddress, IAddressCreate } from "./address.interface";
import { IScheduleCreate } from "./schedule.interface";

export{
    
    IUser, 
    IUserCreate, 
    IUserList, 
    IUserReturn, 
    IUserUpdate,
    ILogin,
    ILoginReturn,
    ICategory, 
    ICategoryList, 
    ICreateCategory,
    IRealEstate, 
    IRealEstateList,
    IRealEstateCreate,
    IAddress, 
    IAddressCreate,
    IScheduleCreate
}